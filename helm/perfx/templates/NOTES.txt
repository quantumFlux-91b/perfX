PerfX has been deployed!

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  Release:   {{ .Release.Name }}
  Namespace: {{ .Release.Namespace }}
  Chart:     {{ .Chart.Name }}-{{ .Chart.Version }}
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Check rollout status:
  kubectl rollout status deployment/{{ include "perfx.fullname" . }}-backend -n {{ .Release.Namespace }}
  kubectl rollout status deployment/{{ include "perfx.fullname" . }}-frontend -n {{ .Release.Namespace }}

View all pods:
  kubectl get pods -n {{ .Release.Namespace }} -l app.kubernetes.io/instance={{ .Release.Name }}

{{- if .Values.ingress.enabled }}
Application URL (Ingress):
  http{{ if .Values.ingress.tls.enabled }}s{{ end }}://{{ .Values.ingress.host }}
{{- else if .Values.route.enabled }}
Application URL (OpenShift Route):
  Run: oc get routes -n {{ .Release.Namespace }}
{{- else }}
Port-forward for local access:
  kubectl port-forward -n {{ .Release.Namespace }} svc/{{ include "perfx.fullname" . }}-frontend 8080:8080
  Then open: http://localhost:8080
{{- end }}

Backend logs:
  kubectl logs -n {{ .Release.Namespace }} -l app.kubernetes.io/name={{ include "perfx.name" . }}-backend -f

{{- if .Values.postgres.enabled }}
PostgreSQL logs:
  kubectl logs -n {{ .Release.Namespace }} -l app.kubernetes.io/name={{ include "perfx.name" . }}-postgres -f
{{- end }}
